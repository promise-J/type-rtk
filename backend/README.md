# Backend (Express + TypeScript)

Commands:
- `npm install`
- `npm run dev` (dev server with ts-node + nodemon)
- `npm run build` (compile to dist)
- `npm start` (start compiled dist)

API:
- GET /api/items
- POST /api/items { name: string }
