Fullstack RTK Demo (Vite frontend + Express backend)

- backend: runs on http://localhost:5000
- frontend: runs on http://localhost:3000 (Vite)

Steps:
1. cd backend && npm install && npm run dev
2. cd ../frontend && npm install && npm run dev
# type-rtk
